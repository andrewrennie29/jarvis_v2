<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <meta name="description" content="Jarvis: My Organiser">
   <meta name="author" content="Andrew Rennie">
  <title>Jarvis</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>

<body>

<%= render partial: 'layouts/navbar' %>

<div class = "container">
  <div class = "row">

		<% unless flash.notice.nil? %> <div class="alert alert-success" role="alert"><%= flash.notice %></div> <% end %>
		<div class = "col-md-9"> <% if session[:user_id].nil? %> <%= render partial: 'sessions/login' %> <% else %> <%= yield %> <% end %> </div>
		<div class = "col-md-3"> <%= render partial: 'layouts/sidebar' %> </div>

	</div>
</div>

<div id="new-project-modal" class="modal fade">
	<div class="modal-dialog">
		<div id="inner-new-project-modal" class="modal-content"></div>
	</div>
</div>

<div id="new-todo-modal" class="modal fade">
	<div class="modal-dialog">
		<div id="inner-new-todo-modal" class="modal-content"></div>
	</div>
</div>

</body>
</html>
