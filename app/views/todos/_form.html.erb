<%= form_for @todo, :html => {:class => "form-horizontal"} do |f| %>
  <!-- Modal -->
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">Create New Todo</h4>
      </div>
      <div class="modal-body">
        <% if @todo.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@todo.errors.count, "error") %> prohibited this todo from being saved:</h2>

            <ul>
              <% @todo.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <p><div class="input-group input-group-sm">
          <span class="input-group-addon" id="basic-addon1">Name</span>
          <%= f.text_field :name, :class => "form-control", :placeholder => "todo Name" %>
        </div></p>

        <p>
          <%= f.text_area :details, :class => "form-control", :placeholder => "Enter todo details" %>
        </p>

        <p><div class="input-group input-group-sm">
          <span class="input-group-addon" id="basic-addon1">Name</span>
          <%= f.text_field :duedate, :class => "form-control", :placeholder => "Due Date" %>
        </div></p>

        <p><div class="input-group input-group-sm">
          <span class="input-group-addon" id="basic-addon1">Name</span>
          <%= f.select :importance, :class => "form-control", :placeholder => "Importance" %>
        </div></p>

        <p><div class="input-group input-group-sm">
          <span class="input-group-addon" id="basic-addon1">Name</span>
          <%= f.text_field :timerequired, :class => "form-control", :placeholder => "Time Required" %>
        </div></p>

        <p><div class="input-group input-group-sm">
          <span class="input-group-addon" id="basic-addon1">Project</span>
          <%= f.collection_select :project_id, User.find_by_id(session[:user_id]).projects.where(:isactive => true).order(name: :asc), :id, :name,
           {prompt: "Select Project"}, {class: "form-control"} %>
        </div></p>

        <p><div class="input-group input-group-sm">
          <span class="input-group-addon" id="basic-addon1">Category</span>
          <%= f.collection_select :category_id, Category.all, :id, :name,
           {prompt: "Select Category"}, {class: "form-control"} %>
        </div></p>

      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <%= f.submit "Create Todo", :class => "btn btn-primary" %>
      </div>

<% end %>
